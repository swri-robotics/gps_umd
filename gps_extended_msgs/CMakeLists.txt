cmake_minimum_required(VERSION 3.10)
project(gps_extended_msgs)

set(CMAKE_CXX_STANDARD 14)

set(MSG_DEPS std_msgs builtin_interfaces)

find_package(ament_cmake REQUIRED)
find_package(rosidl_default_generators REQUIRED)
find_package(std_msgs REQUIRED)
find_package(builtin_interfaces REQUIRED)

# Every message here is generated by tools/generate_raw_msgs.py, one set per
# gpsd C API (MAJOR, MINOR) pair. They are globbed rather than listed so
# regenerating does not also require editing this file; CI runs the generator
# with --check so the checked-in set cannot drift.
#
# These live apart from gps_msgs deliberately. gps_msgs is a small, long-
# released package (GPSFix, GPSStatus) built by the ROS build farm for five
# distros; this one is roughly 900 messages and takes minutes to build, so the
# cost stays with the feature that incurs it.
#
# Note they are built unconditionally, with no libgps dependency: this is a
# pure interface package and must stay buildable everywhere.
file(GLOB MSG_FILES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} msg/GPSExtended*.msg)
list(SORT MSG_FILES)
list(LENGTH MSG_FILES MSG_COUNT)
message(STATUS "gps_extended_msgs: generating ${MSG_COUNT} messages")

rosidl_generate_interfaces(${PROJECT_NAME}
  ${MSG_FILES}
  DEPENDENCIES ${MSG_DEPS}
)

ament_export_dependencies(rosidl_default_runtime)

#############
## Testing ##
#############

if(BUILD_TESTING)
  find_package(ament_cmake_pytest REQUIRED)

  # The generator's tests live with the generator rather than with a package,
  # and run here because this package owns its output.
  #
  # Neither suite links or needs libgps -- they read gps.h out of a gpsd git
  # clone and skip cleanly when it is absent, so this package stays buildable
  # and testable on the build farm. Point GPSD_REPO at a clone to run them:
  #
  #   GPSD_REPO=/path/to/gpsd colcon test --packages-select gps_extended_msgs
  #
  # tools/test_against_gpsd.sh maintains one at <workspace>/.gpsd_versions/gpsd,
  # which is the default.
  set(GENERATOR_TESTS
    test_generate_raw_msgs.py      # generator internals, synthetic fragments
    test_generated_messages.py     # generated output vs. the real gps.h
  )
  foreach(test_file ${GENERATOR_TESTS})
    ament_add_pytest_test(${test_file}
      "${CMAKE_CURRENT_SOURCE_DIR}/../tools/${test_file}"
      TIMEOUT 600)
  endforeach()
endif()

ament_package()
