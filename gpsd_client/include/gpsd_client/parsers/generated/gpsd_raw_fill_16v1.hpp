// Generated by tools/generate_raw_msgs.py from GPSd release-3.27.5 (libgps API 16.1).
// Do not edit; edit the generator and regenerate.
#ifndef GPSD_CLIENT__PARSERS__GENERATED__FILL_16V1_HPP_
#define GPSD_CLIENT__PARSERS__GENERATED__FILL_16V1_HPP_

#include <gpsd_client/parsers/generated/gpsd_has_member.hpp>

#include <gps_extended_msgs/msg/gpsd_raw16v1.hpp>

#include <gps.h>

#include <cstddef>
#include <cstring>
#include <iterator>
#include <type_traits>
#include <vector>

// Which pair this build actually uses. gpsd_raw_message.hpp sets these
// before including one fill header, so a libgps newer than anything
// tested can still be pointed at the newest parser. Defaulted here so
// the header stays usable on its own.
#ifndef GPSD_RAW_FILL_MAJOR
#define GPSD_RAW_FILL_MAJOR GPSD_API_MAJOR_VERSION
#endif
#ifndef GPSD_RAW_FILL_MINOR
#define GPSD_RAW_FILL_MINOR GPSD_API_MINOR_VERSION
#endif

#if GPSD_RAW_FILL_MAJOR == 16 && GPSD_RAW_FILL_MINOR == 1

namespace gpsd_client
{
namespace generated
{

// The message's mask constants are GPSd's own bit values under a
// name the preprocessor leaves alone. Checked here, where both
// spellings are legitimately in scope, so a rename that changes a
// value cannot reach a subscriber.
#ifdef ONLINE_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_ONLINE == static_cast<uint64_t>(ONLINE_SET),
              "SET_ONLINE disagrees with gps.h's ONLINE_SET");
#endif
#ifdef TIME_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_TIME == static_cast<uint64_t>(TIME_SET),
              "SET_TIME disagrees with gps.h's TIME_SET");
#endif
#ifdef TIMERR_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_TIMERR == static_cast<uint64_t>(TIMERR_SET),
              "SET_TIMERR disagrees with gps.h's TIMERR_SET");
#endif
#ifdef LATLON_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_LATLON == static_cast<uint64_t>(LATLON_SET),
              "SET_LATLON disagrees with gps.h's LATLON_SET");
#endif
#ifdef ALTITUDE_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_ALTITUDE == static_cast<uint64_t>(ALTITUDE_SET),
              "SET_ALTITUDE disagrees with gps.h's ALTITUDE_SET");
#endif
#ifdef SPEED_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_SPEED == static_cast<uint64_t>(SPEED_SET),
              "SET_SPEED disagrees with gps.h's SPEED_SET");
#endif
#ifdef TRACK_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_TRACK == static_cast<uint64_t>(TRACK_SET),
              "SET_TRACK disagrees with gps.h's TRACK_SET");
#endif
#ifdef CLIMB_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_CLIMB == static_cast<uint64_t>(CLIMB_SET),
              "SET_CLIMB disagrees with gps.h's CLIMB_SET");
#endif
#ifdef STATUS_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_STATUS == static_cast<uint64_t>(STATUS_SET),
              "SET_STATUS disagrees with gps.h's STATUS_SET");
#endif
#ifdef MODE_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_MODE == static_cast<uint64_t>(MODE_SET),
              "SET_MODE disagrees with gps.h's MODE_SET");
#endif
#ifdef DOP_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_DOP == static_cast<uint64_t>(DOP_SET),
              "SET_DOP disagrees with gps.h's DOP_SET");
#endif
#ifdef HERR_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_HERR == static_cast<uint64_t>(HERR_SET),
              "SET_HERR disagrees with gps.h's HERR_SET");
#endif
#ifdef VERR_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_VERR == static_cast<uint64_t>(VERR_SET),
              "SET_VERR disagrees with gps.h's VERR_SET");
#endif
#ifdef ATTITUDE_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_ATTITUDE == static_cast<uint64_t>(ATTITUDE_SET),
              "SET_ATTITUDE disagrees with gps.h's ATTITUDE_SET");
#endif
#ifdef SATELLITE_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_SATELLITE == static_cast<uint64_t>(SATELLITE_SET),
              "SET_SATELLITE disagrees with gps.h's SATELLITE_SET");
#endif
#ifdef SPEEDERR_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_SPEEDERR == static_cast<uint64_t>(SPEEDERR_SET),
              "SET_SPEEDERR disagrees with gps.h's SPEEDERR_SET");
#endif
#ifdef TRACKERR_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_TRACKERR == static_cast<uint64_t>(TRACKERR_SET),
              "SET_TRACKERR disagrees with gps.h's TRACKERR_SET");
#endif
#ifdef CLIMBERR_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_CLIMBERR == static_cast<uint64_t>(CLIMBERR_SET),
              "SET_CLIMBERR disagrees with gps.h's CLIMBERR_SET");
#endif
#ifdef DEVICE_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_DEVICE == static_cast<uint64_t>(DEVICE_SET),
              "SET_DEVICE disagrees with gps.h's DEVICE_SET");
#endif
#ifdef DEVICELIST_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_DEVICELIST == static_cast<uint64_t>(DEVICELIST_SET),
              "SET_DEVICELIST disagrees with gps.h's DEVICELIST_SET");
#endif
#ifdef DEVICEID_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_DEVICEID == static_cast<uint64_t>(DEVICEID_SET),
              "SET_DEVICEID disagrees with gps.h's DEVICEID_SET");
#endif
#ifdef RTCM2_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_RTCM2 == static_cast<uint64_t>(RTCM2_SET),
              "SET_RTCM2 disagrees with gps.h's RTCM2_SET");
#endif
#ifdef RTCM3_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_RTCM3 == static_cast<uint64_t>(RTCM3_SET),
              "SET_RTCM3 disagrees with gps.h's RTCM3_SET");
#endif
#ifdef AIS_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_AIS == static_cast<uint64_t>(AIS_SET),
              "SET_AIS disagrees with gps.h's AIS_SET");
#endif
#ifdef PACKET_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_PACKET == static_cast<uint64_t>(PACKET_SET),
              "SET_PACKET disagrees with gps.h's PACKET_SET");
#endif
#ifdef SUBFRAME_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_SUBFRAME == static_cast<uint64_t>(SUBFRAME_SET),
              "SET_SUBFRAME disagrees with gps.h's SUBFRAME_SET");
#endif
#ifdef GST_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_GST == static_cast<uint64_t>(GST_SET),
              "SET_GST disagrees with gps.h's GST_SET");
#endif
#ifdef VERSION_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_VERSION == static_cast<uint64_t>(VERSION_SET),
              "SET_VERSION disagrees with gps.h's VERSION_SET");
#endif
#ifdef POLICY_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_POLICY == static_cast<uint64_t>(POLICY_SET),
              "SET_POLICY disagrees with gps.h's POLICY_SET");
#endif
#ifdef LOGMESSAGE_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_LOGMESSAGE == static_cast<uint64_t>(LOGMESSAGE_SET),
              "SET_LOGMESSAGE disagrees with gps.h's LOGMESSAGE_SET");
#endif
#ifdef ERROR_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_ERROR == static_cast<uint64_t>(ERROR_SET),
              "SET_ERROR disagrees with gps.h's ERROR_SET");
#endif
#ifdef TOFF_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_TOFF == static_cast<uint64_t>(TOFF_SET),
              "SET_TOFF disagrees with gps.h's TOFF_SET");
#endif
#ifdef PPS_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_PPS == static_cast<uint64_t>(PPS_SET),
              "SET_PPS disagrees with gps.h's PPS_SET");
#endif
#ifdef NAVDATA_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_NAVDATA == static_cast<uint64_t>(NAVDATA_SET),
              "SET_NAVDATA disagrees with gps.h's NAVDATA_SET");
#endif
#ifdef OSCILLATOR_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_OSCILLATOR == static_cast<uint64_t>(OSCILLATOR_SET),
              "SET_OSCILLATOR disagrees with gps.h's OSCILLATOR_SET");
#endif
#ifdef ECEF_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_ECEF == static_cast<uint64_t>(ECEF_SET),
              "SET_ECEF disagrees with gps.h's ECEF_SET");
#endif
#ifdef VECEF_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_VECEF == static_cast<uint64_t>(VECEF_SET),
              "SET_VECEF disagrees with gps.h's VECEF_SET");
#endif
#ifdef MAGNETIC_TRACK_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_MAGNETIC_TRACK == static_cast<uint64_t>(MAGNETIC_TRACK_SET),
              "SET_MAGNETIC_TRACK disagrees with gps.h's MAGNETIC_TRACK_SET");
#endif
#ifdef RAW_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_RAW == static_cast<uint64_t>(RAW_SET),
              "SET_RAW disagrees with gps.h's RAW_SET");
#endif
#ifdef NED_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_NED == static_cast<uint64_t>(NED_SET),
              "SET_NED disagrees with gps.h's NED_SET");
#endif
#ifdef VNED_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_VNED == static_cast<uint64_t>(VNED_SET),
              "SET_VNED disagrees with gps.h's VNED_SET");
#endif
#ifdef LOG_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_LOG == static_cast<uint64_t>(LOG_SET),
              "SET_LOG disagrees with gps.h's LOG_SET");
#endif
#ifdef IMU_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_IMU == static_cast<uint64_t>(IMU_SET),
              "SET_IMU disagrees with gps.h's IMU_SET");
#endif
#ifdef EOF_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_EOF == static_cast<uint64_t>(EOF_SET),
              "SET_EOF disagrees with gps.h's EOF_SET");
#endif
#ifdef SPARTN_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_SPARTN == static_cast<uint64_t>(SPARTN_SET),
              "SET_SPARTN disagrees with gps.h's SPARTN_SET");
#endif
#ifdef UNION_SET
static_assert(gps_extended_msgs::msg::GPSDRaw16v1::SET_UNION == static_cast<uint64_t>(UNION_SET),
              "SET_UNION disagrees with gps.h's UNION_SET");
#endif

GPSD_DEFINE_HAS_MEMBER(NED)
GPSD_DEFINE_HAS_MEMBER(PRN)
GPSD_DEFINE_HAS_MEMBER(acc_len)
GPSD_DEFINE_HAS_MEMBER(acc_x)
GPSD_DEFINE_HAS_MEMBER(acc_y)
GPSD_DEFINE_HAS_MEMBER(acc_z)
GPSD_DEFINE_HAS_MEMBER(activated)
GPSD_DEFINE_HAS_MEMBER(altHAE)
GPSD_DEFINE_HAS_MEMBER(altMSL)
GPSD_DEFINE_HAS_MEMBER(alt_err_deviation)
GPSD_DEFINE_HAS_MEMBER(altitude)
GPSD_DEFINE_HAS_MEMBER(ant_power)
GPSD_DEFINE_HAS_MEMBER(ant_stat)
GPSD_DEFINE_HAS_MEMBER(attitude)
GPSD_DEFINE_HAS_MEMBER(azimuth)
GPSD_DEFINE_HAS_MEMBER(base)
GPSD_DEFINE_HAS_MEMBER(baudrate)
GPSD_DEFINE_HAS_MEMBER(c2c)
GPSD_DEFINE_HAS_MEMBER(carrierphase)
GPSD_DEFINE_HAS_MEMBER(climb)
GPSD_DEFINE_HAS_MEMBER(clock)
GPSD_DEFINE_HAS_MEMBER(clockbias)
GPSD_DEFINE_HAS_MEMBER(clockdrift)
GPSD_DEFINE_HAS_MEMBER(codephase)
GPSD_DEFINE_HAS_MEMBER(course)
GPSD_DEFINE_HAS_MEMBER(cycle)
GPSD_DEFINE_HAS_MEMBER(datum)
GPSD_DEFINE_HAS_MEMBER(delta)
GPSD_DEFINE_HAS_MEMBER(deltarange)
GPSD_DEFINE_HAS_MEMBER(depth)
GPSD_DEFINE_HAS_MEMBER(dev)
GPSD_DEFINE_HAS_MEMBER(devices)
GPSD_DEFINE_HAS_MEMBER(devpath)
GPSD_DEFINE_HAS_MEMBER(dgps_age)
GPSD_DEFINE_HAS_MEMBER(dgps_station)
GPSD_DEFINE_HAS_MEMBER(dip)
GPSD_DEFINE_HAS_MEMBER(disciplined)
GPSD_DEFINE_HAS_MEMBER(distance)
GPSD_DEFINE_HAS_MEMBER(distanceStd)
GPSD_DEFINE_HAS_MEMBER(dop)
GPSD_DEFINE_HAS_MEMBER(doppler)
GPSD_DEFINE_HAS_MEMBER(driver)
GPSD_DEFINE_HAS_MEMBER(driver_mode)
GPSD_DEFINE_HAS_MEMBER(east)
GPSD_DEFINE_HAS_MEMBER(ecef)
GPSD_DEFINE_HAS_MEMBER(elevation)
GPSD_DEFINE_HAS_MEMBER(epc)
GPSD_DEFINE_HAS_MEMBER(epd)
GPSD_DEFINE_HAS_MEMBER(eph)
GPSD_DEFINE_HAS_MEMBER(eps)
GPSD_DEFINE_HAS_MEMBER(ept)
GPSD_DEFINE_HAS_MEMBER(epv)
GPSD_DEFINE_HAS_MEMBER(epx)
GPSD_DEFINE_HAS_MEMBER(epy)
GPSD_DEFINE_HAS_MEMBER(errEllipseMajor)
GPSD_DEFINE_HAS_MEMBER(errEllipseMinor)
GPSD_DEFINE_HAS_MEMBER(errEllipseOrient)
GPSD_DEFINE_HAS_MEMBER(error)
GPSD_DEFINE_HAS_MEMBER(fix)
GPSD_DEFINE_HAS_MEMBER(fixType)
GPSD_DEFINE_HAS_MEMBER(flags)
GPSD_DEFINE_HAS_MEMBER(freqid)
GPSD_DEFINE_HAS_MEMBER(gSpeed)
GPSD_DEFINE_HAS_MEMBER(gdop)
GPSD_DEFINE_HAS_MEMBER(geoid_sep)
GPSD_DEFINE_HAS_MEMBER(gnssid)
GPSD_DEFINE_HAS_MEMBER(gst)
GPSD_DEFINE_HAS_MEMBER(gyro_temp)
GPSD_DEFINE_HAS_MEMBER(gyro_x)
GPSD_DEFINE_HAS_MEMBER(gyro_y)
GPSD_DEFINE_HAS_MEMBER(gyro_z)
GPSD_DEFINE_HAS_MEMBER(hAcc)
GPSD_DEFINE_HAS_MEMBER(hdop)
GPSD_DEFINE_HAS_MEMBER(headAcc)
GPSD_DEFINE_HAS_MEMBER(heading)
GPSD_DEFINE_HAS_MEMBER(health)
GPSD_DEFINE_HAS_MEMBER(hexdata)
GPSD_DEFINE_HAS_MEMBER(imu)
GPSD_DEFINE_HAS_MEMBER(index_cnt)
GPSD_DEFINE_HAS_MEMBER(jam)
GPSD_DEFINE_HAS_MEMBER(json)
GPSD_DEFINE_HAS_MEMBER(l2c)
GPSD_DEFINE_HAS_MEMBER(lat)
GPSD_DEFINE_HAS_MEMBER(lat_err_deviation)
GPSD_DEFINE_HAS_MEMBER(latitude)
GPSD_DEFINE_HAS_MEMBER(leap_seconds)
GPSD_DEFINE_HAS_MEMBER(length)
GPSD_DEFINE_HAS_MEMBER(list)
GPSD_DEFINE_HAS_MEMBER(lli)
GPSD_DEFINE_HAS_MEMBER(locktime)
GPSD_DEFINE_HAS_MEMBER(log)
GPSD_DEFINE_HAS_MEMBER(loglevel)
GPSD_DEFINE_HAS_MEMBER(lon)
GPSD_DEFINE_HAS_MEMBER(lon_err_deviation)
GPSD_DEFINE_HAS_MEMBER(longitude)
GPSD_DEFINE_HAS_MEMBER(mag_len)
GPSD_DEFINE_HAS_MEMBER(mag_st)
GPSD_DEFINE_HAS_MEMBER(mag_x)
GPSD_DEFINE_HAS_MEMBER(mag_y)
GPSD_DEFINE_HAS_MEMBER(mag_z)
GPSD_DEFINE_HAS_MEMBER(magnetic_track)
GPSD_DEFINE_HAS_MEMBER(magnetic_var)
GPSD_DEFINE_HAS_MEMBER(meas)
GPSD_DEFINE_HAS_MEMBER(mheading)
GPSD_DEFINE_HAS_MEMBER(mincycle)
GPSD_DEFINE_HAS_MEMBER(mode)
GPSD_DEFINE_HAS_MEMBER(msg)
GPSD_DEFINE_HAS_MEMBER(mtime)
GPSD_DEFINE_HAS_MEMBER(ndevices)
GPSD_DEFINE_HAS_MEMBER(nmea)
GPSD_DEFINE_HAS_MEMBER(north)
GPSD_DEFINE_HAS_MEMBER(numSV)
GPSD_DEFINE_HAS_MEMBER(obs_code)
GPSD_DEFINE_HAS_MEMBER(online)
GPSD_DEFINE_HAS_MEMBER(osc)
GPSD_DEFINE_HAS_MEMBER(pAcc)
GPSD_DEFINE_HAS_MEMBER(pDOP)
GPSD_DEFINE_HAS_MEMBER(parity)
GPSD_DEFINE_HAS_MEMBER(path)
GPSD_DEFINE_HAS_MEMBER(pdop)
GPSD_DEFINE_HAS_MEMBER(pitch)
GPSD_DEFINE_HAS_MEMBER(pitch_st)
GPSD_DEFINE_HAS_MEMBER(policy)
GPSD_DEFINE_HAS_MEMBER(pps)
GPSD_DEFINE_HAS_MEMBER(pr)
GPSD_DEFINE_HAS_MEMBER(prRate)
GPSD_DEFINE_HAS_MEMBER(prRes)
GPSD_DEFINE_HAS_MEMBER(proto_major)
GPSD_DEFINE_HAS_MEMBER(proto_minor)
GPSD_DEFINE_HAS_MEMBER(pseudorange)
GPSD_DEFINE_HAS_MEMBER(qErr)
GPSD_DEFINE_HAS_MEMBER(qErr_time)
GPSD_DEFINE_HAS_MEMBER(qualityInd)
GPSD_DEFINE_HAS_MEMBER(ratio)
GPSD_DEFINE_HAS_MEMBER(raw)
GPSD_DEFINE_HAS_MEMBER(real)
GPSD_DEFINE_HAS_MEMBER(reference)
GPSD_DEFINE_HAS_MEMBER(relPosD)
GPSD_DEFINE_HAS_MEMBER(relPosE)
GPSD_DEFINE_HAS_MEMBER(relPosH)
GPSD_DEFINE_HAS_MEMBER(relPosL)
GPSD_DEFINE_HAS_MEMBER(relPosN)
GPSD_DEFINE_HAS_MEMBER(release)
GPSD_DEFINE_HAS_MEMBER(remote)
GPSD_DEFINE_HAS_MEMBER(rev)
GPSD_DEFINE_HAS_MEMBER(rms_deviation)
GPSD_DEFINE_HAS_MEMBER(roll)
GPSD_DEFINE_HAS_MEMBER(roll_st)
GPSD_DEFINE_HAS_MEMBER(rot)
GPSD_DEFINE_HAS_MEMBER(running)
GPSD_DEFINE_HAS_MEMBER(sAcc)
GPSD_DEFINE_HAS_MEMBER(satellites_used)
GPSD_DEFINE_HAS_MEMBER(satellites_visible)
GPSD_DEFINE_HAS_MEMBER(satstat)
GPSD_DEFINE_HAS_MEMBER(scaled)
GPSD_DEFINE_HAS_MEMBER(sep)
GPSD_DEFINE_HAS_MEMBER(sernum)
GPSD_DEFINE_HAS_MEMBER(set)
GPSD_DEFINE_HAS_MEMBER(sigid)
GPSD_DEFINE_HAS_MEMBER(skyview)
GPSD_DEFINE_HAS_MEMBER(skyview_time)
GPSD_DEFINE_HAS_MEMBER(smajor_deviation)
GPSD_DEFINE_HAS_MEMBER(smajor_orientation)
GPSD_DEFINE_HAS_MEMBER(sminor_deviation)
GPSD_DEFINE_HAS_MEMBER(snr)
GPSD_DEFINE_HAS_MEMBER(source)
GPSD_DEFINE_HAS_MEMBER(spec)
GPSD_DEFINE_HAS_MEMBER(speed)
GPSD_DEFINE_HAS_MEMBER(split24)
GPSD_DEFINE_HAS_MEMBER(ss)
GPSD_DEFINE_HAS_MEMBER(status)
GPSD_DEFINE_HAS_MEMBER(stopbits)
GPSD_DEFINE_HAS_MEMBER(string)
GPSD_DEFINE_HAS_MEMBER(subtype)
GPSD_DEFINE_HAS_MEMBER(subtype1)
GPSD_DEFINE_HAS_MEMBER(svid)
GPSD_DEFINE_HAS_MEMBER(tAcc)
GPSD_DEFINE_HAS_MEMBER(tdop)
GPSD_DEFINE_HAS_MEMBER(temp)
GPSD_DEFINE_HAS_MEMBER(then)
GPSD_DEFINE_HAS_MEMBER(time)
GPSD_DEFINE_HAS_MEMBER(timeTag)
GPSD_DEFINE_HAS_MEMBER(timing)
GPSD_DEFINE_HAS_MEMBER(toff)
GPSD_DEFINE_HAS_MEMBER(totalDistance)
GPSD_DEFINE_HAS_MEMBER(track)
GPSD_DEFINE_HAS_MEMBER(up)
GPSD_DEFINE_HAS_MEMBER(used)
GPSD_DEFINE_HAS_MEMBER(utctime)
GPSD_DEFINE_HAS_MEMBER(vAcc)
GPSD_DEFINE_HAS_MEMBER(vdop)
GPSD_DEFINE_HAS_MEMBER(ve_err_deviation)
GPSD_DEFINE_HAS_MEMBER(velD)
GPSD_DEFINE_HAS_MEMBER(velE)
GPSD_DEFINE_HAS_MEMBER(velN)
GPSD_DEFINE_HAS_MEMBER(version)
GPSD_DEFINE_HAS_MEMBER(vn_err_deviation)
GPSD_DEFINE_HAS_MEMBER(vu_err_deviation)
GPSD_DEFINE_HAS_MEMBER(vx)
GPSD_DEFINE_HAS_MEMBER(vy)
GPSD_DEFINE_HAS_MEMBER(vz)
GPSD_DEFINE_HAS_MEMBER(wanglem)
GPSD_DEFINE_HAS_MEMBER(wangler)
GPSD_DEFINE_HAS_MEMBER(wanglet)
GPSD_DEFINE_HAS_MEMBER(watch)
GPSD_DEFINE_HAS_MEMBER(watcher)
GPSD_DEFINE_HAS_MEMBER(wspeedr)
GPSD_DEFINE_HAS_MEMBER(wspeedt)
GPSD_DEFINE_HAS_MEMBER(wtemp)
GPSD_DEFINE_HAS_MEMBER(x)
GPSD_DEFINE_HAS_MEMBER(xdop)
GPSD_DEFINE_HAS_MEMBER(y)
GPSD_DEFINE_HAS_MEMBER(yaw)
GPSD_DEFINE_HAS_MEMBER(yaw_st)
GPSD_DEFINE_HAS_MEMBER(ydop)
GPSD_DEFINE_HAS_MEMBER(z)

// Forward declarations; see the note in the generator.
template <typename T>
inline void fill(const T& in, gps_extended_msgs::msg::GPSDRaw16v1& out);

template <typename T>
inline void fill(const T& in, gps_extended_msgs::msg::GPSDRaw16v1& out)
{
  (void)in;
  (void)out;
  if constexpr (has_set<T>::value) {
    out.set = in.set;
  }
  if constexpr (has_online<T>::value) {
    out.online.sec = static_cast<int32_t>(in.online.tv_sec);
    out.online.nanosec = static_cast<uint32_t>(in.online.tv_nsec);
  }
  if constexpr (has_fix<T>::value) {
    using T_fix = std::decay_t<decltype(in.fix)>;
    if constexpr (has_time<T_fix>::value) {
      out.fix_time.sec = static_cast<int32_t>(in.fix.time.tv_sec);
      out.fix_time.nanosec = static_cast<uint32_t>(in.fix.time.tv_nsec);
    }
    if constexpr (has_mode<T_fix>::value) {
      out.fix_mode = in.fix.mode;
    }
    if constexpr (has_status<T_fix>::value) {
      out.fix_status = in.fix.status;
    }
    if constexpr (has_ept<T_fix>::value) {
      out.fix_ept = in.fix.ept;
    }
    if constexpr (has_latitude<T_fix>::value) {
      out.fix_latitude = in.fix.latitude;
    }
    if constexpr (has_epy<T_fix>::value) {
      out.fix_epy = in.fix.epy;
    }
    if constexpr (has_longitude<T_fix>::value) {
      out.fix_longitude = in.fix.longitude;
    }
    if constexpr (has_epx<T_fix>::value) {
      out.fix_epx = in.fix.epx;
    }
    if constexpr (has_altitude<T_fix>::value) {
      out.fix_altitude = in.fix.altitude;
    }
    if constexpr (has_altHAE<T_fix>::value) {
      out.fix_alt_hae = in.fix.altHAE;
    }
    if constexpr (has_altMSL<T_fix>::value) {
      out.fix_alt_msl = in.fix.altMSL;
    }
    if constexpr (has_epv<T_fix>::value) {
      out.fix_epv = in.fix.epv;
    }
    if constexpr (has_track<T_fix>::value) {
      out.fix_track = in.fix.track;
    }
    if constexpr (has_epd<T_fix>::value) {
      out.fix_epd = in.fix.epd;
    }
    if constexpr (has_speed<T_fix>::value) {
      out.fix_speed = in.fix.speed;
    }
    if constexpr (has_eps<T_fix>::value) {
      out.fix_eps = in.fix.eps;
    }
    if constexpr (has_climb<T_fix>::value) {
      out.fix_climb = in.fix.climb;
    }
    if constexpr (has_epc<T_fix>::value) {
      out.fix_epc = in.fix.epc;
    }
    if constexpr (has_eph<T_fix>::value) {
      out.fix_eph = in.fix.eph;
    }
    if constexpr (has_sep<T_fix>::value) {
      out.fix_sep = in.fix.sep;
    }
    if constexpr (has_geoid_sep<T_fix>::value) {
      out.fix_geoid_sep = in.fix.geoid_sep;
    }
    if constexpr (has_magnetic_track<T_fix>::value) {
      out.fix_magnetic_track = in.fix.magnetic_track;
    }
    if constexpr (has_magnetic_var<T_fix>::value) {
      out.fix_magnetic_var = in.fix.magnetic_var;
    }
    if constexpr (has_depth<T_fix>::value) {
      out.fix_depth = in.fix.depth;
    }
    if constexpr (has_wtemp<T_fix>::value) {
      out.fix_wtemp = in.fix.wtemp;
    }
    if constexpr (has_temp<T_fix>::value) {
      out.fix_temp = in.fix.temp;
    }
    if constexpr (has_ant_stat<T_fix>::value) {
      out.fix_ant_stat = in.fix.ant_stat;
    }
    if constexpr (has_ant_power<T_fix>::value) {
      out.fix_ant_power = in.fix.ant_power;
    }
    if constexpr (has_jam<T_fix>::value) {
      out.fix_jam = in.fix.jam;
    }
    if constexpr (has_clockbias<T_fix>::value) {
      out.fix_clockbias = in.fix.clockbias;
    }
    if constexpr (has_clockdrift<T_fix>::value) {
      out.fix_clockdrift = in.fix.clockdrift;
    }
    if constexpr (has_ecef<T_fix>::value) {
      using T_fix_ecef = std::decay_t<decltype(in.fix.ecef)>;
      if constexpr (has_x<T_fix_ecef>::value) {
        out.fix_ecef_x = in.fix.ecef.x;
      }
      if constexpr (has_y<T_fix_ecef>::value) {
        out.fix_ecef_y = in.fix.ecef.y;
      }
      if constexpr (has_z<T_fix_ecef>::value) {
        out.fix_ecef_z = in.fix.ecef.z;
      }
      if constexpr (has_pAcc<T_fix_ecef>::value) {
        out.fix_ecef_p_acc = in.fix.ecef.pAcc;
      }
      if constexpr (has_vx<T_fix_ecef>::value) {
        out.fix_ecef_vx = in.fix.ecef.vx;
      }
      if constexpr (has_vy<T_fix_ecef>::value) {
        out.fix_ecef_vy = in.fix.ecef.vy;
      }
      if constexpr (has_vz<T_fix_ecef>::value) {
        out.fix_ecef_vz = in.fix.ecef.vz;
      }
      if constexpr (has_vAcc<T_fix_ecef>::value) {
        out.fix_ecef_v_acc = in.fix.ecef.vAcc;
      }
    }
    if constexpr (has_NED<T_fix>::value) {
      using T_fix_NED = std::decay_t<decltype(in.fix.NED)>;
      if constexpr (has_relPosN<T_fix_NED>::value) {
        out.fix_ned_rel_pos_n = in.fix.NED.relPosN;
      }
      if constexpr (has_relPosE<T_fix_NED>::value) {
        out.fix_ned_rel_pos_e = in.fix.NED.relPosE;
      }
      if constexpr (has_relPosD<T_fix_NED>::value) {
        out.fix_ned_rel_pos_d = in.fix.NED.relPosD;
      }
      if constexpr (has_relPosL<T_fix_NED>::value) {
        out.fix_ned_rel_pos_l = in.fix.NED.relPosL;
      }
      if constexpr (has_relPosH<T_fix_NED>::value) {
        out.fix_ned_rel_pos_h = in.fix.NED.relPosH;
      }
      if constexpr (has_velN<T_fix_NED>::value) {
        out.fix_ned_vel_n = in.fix.NED.velN;
      }
      if constexpr (has_velE<T_fix_NED>::value) {
        out.fix_ned_vel_e = in.fix.NED.velE;
      }
      if constexpr (has_velD<T_fix_NED>::value) {
        out.fix_ned_vel_d = in.fix.NED.velD;
      }
    }
    if constexpr (has_datum<T_fix>::value) {
      out.fix_datum.assign(in.fix.datum, strnlen(in.fix.datum, sizeof(in.fix.datum)));
    }
    if constexpr (has_dgps_age<T_fix>::value) {
      out.fix_dgps_age = in.fix.dgps_age;
    }
    if constexpr (has_dgps_station<T_fix>::value) {
      out.fix_dgps_station = in.fix.dgps_station;
    }
    if constexpr (has_wanglem<T_fix>::value) {
      out.fix_wanglem = in.fix.wanglem;
    }
    if constexpr (has_wangler<T_fix>::value) {
      out.fix_wangler = in.fix.wangler;
    }
    if constexpr (has_wanglet<T_fix>::value) {
      out.fix_wanglet = in.fix.wanglet;
    }
    if constexpr (has_wspeedr<T_fix>::value) {
      out.fix_wspeedr = in.fix.wspeedr;
    }
    if constexpr (has_wspeedt<T_fix>::value) {
      out.fix_wspeedt = in.fix.wspeedt;
    }
    if constexpr (has_errEllipseOrient<T_fix>::value) {
      out.fix_err_ellipse_orient = in.fix.errEllipseOrient;
    }
    if constexpr (has_errEllipseMajor<T_fix>::value) {
      out.fix_err_ellipse_major = in.fix.errEllipseMajor;
    }
    if constexpr (has_errEllipseMinor<T_fix>::value) {
      out.fix_err_ellipse_minor = in.fix.errEllipseMinor;
    }
    if constexpr (has_base<T_fix>::value) {
      using T_fix_base = std::decay_t<decltype(in.fix.base)>;
      if constexpr (has_status<T_fix_base>::value) {
        out.fix_base_status = in.fix.base.status;
      }
      if constexpr (has_east<T_fix_base>::value) {
        out.fix_base_east = in.fix.base.east;
      }
      if constexpr (has_north<T_fix_base>::value) {
        out.fix_base_north = in.fix.base.north;
      }
      if constexpr (has_up<T_fix_base>::value) {
        out.fix_base_up = in.fix.base.up;
      }
      if constexpr (has_length<T_fix_base>::value) {
        out.fix_base_length = in.fix.base.length;
      }
      if constexpr (has_course<T_fix_base>::value) {
        out.fix_base_course = in.fix.base.course;
      }
      if constexpr (has_ratio<T_fix_base>::value) {
        out.fix_base_ratio = in.fix.base.ratio;
      }
    }
  }
  if constexpr (has_log<T>::value) {
    using T_log = std::decay_t<decltype(in.log)>;
    if constexpr (has_lon<T_log>::value) {
      out.log_lon = in.log.lon;
    }
    if constexpr (has_lat<T_log>::value) {
      out.log_lat = in.log.lat;
    }
    if constexpr (has_altHAE<T_log>::value) {
      out.log_alt_hae = in.log.altHAE;
    }
    if constexpr (has_altMSL<T_log>::value) {
      out.log_alt_msl = in.log.altMSL;
    }
    if constexpr (has_gSpeed<T_log>::value) {
      out.log_g_speed = in.log.gSpeed;
    }
    if constexpr (has_heading<T_log>::value) {
      out.log_heading = in.log.heading;
    }
    if constexpr (has_tAcc<T_log>::value) {
      out.log_t_acc = in.log.tAcc;
    }
    if constexpr (has_hAcc<T_log>::value) {
      out.log_h_acc = in.log.hAcc;
    }
    if constexpr (has_vAcc<T_log>::value) {
      out.log_v_acc = in.log.vAcc;
    }
    if constexpr (has_sAcc<T_log>::value) {
      out.log_s_acc = in.log.sAcc;
    }
    if constexpr (has_headAcc<T_log>::value) {
      out.log_head_acc = in.log.headAcc;
    }
    if constexpr (has_velN<T_log>::value) {
      out.log_vel_n = in.log.velN;
    }
    if constexpr (has_velE<T_log>::value) {
      out.log_vel_e = in.log.velE;
    }
    if constexpr (has_velD<T_log>::value) {
      out.log_vel_d = in.log.velD;
    }
    if constexpr (has_pDOP<T_log>::value) {
      out.log_p_dop = in.log.pDOP;
    }
    if constexpr (has_distance<T_log>::value) {
      out.log_distance = in.log.distance;
    }
    if constexpr (has_totalDistance<T_log>::value) {
      out.log_total_distance = in.log.totalDistance;
    }
    if constexpr (has_distanceStd<T_log>::value) {
      out.log_distance_std = in.log.distanceStd;
    }
    if constexpr (has_then<T_log>::value) {
      out.log_then.sec = static_cast<int32_t>(in.log.then.tv_sec);
      out.log_then.nanosec = static_cast<uint32_t>(in.log.then.tv_nsec);
    }
    if constexpr (has_status<T_log>::value) {
      out.log_status = in.log.status;
    }
    if constexpr (has_index_cnt<T_log>::value) {
      out.log_index_cnt = in.log.index_cnt;
    }
    if constexpr (has_fixType<T_log>::value) {
      out.log_fix_type = in.log.fixType;
    }
    if constexpr (has_numSV<T_log>::value) {
      out.log_num_sv = in.log.numSV;
    }
    if constexpr (has_string<T_log>::value) {
      out.log_string.assign(in.log.string, strnlen(in.log.string, sizeof(in.log.string)));
    }
  }
  if constexpr (has_leap_seconds<T>::value) {
    out.leap_seconds = in.leap_seconds;
  }
  if constexpr (has_satellites_used<T>::value) {
    out.satellites_used = in.satellites_used;
  }
  if constexpr (has_dop<T>::value) {
    using T_dop = std::decay_t<decltype(in.dop)>;
    if constexpr (has_xdop<T_dop>::value) {
      out.dop_xdop = in.dop.xdop;
    }
    if constexpr (has_ydop<T_dop>::value) {
      out.dop_ydop = in.dop.ydop;
    }
    if constexpr (has_pdop<T_dop>::value) {
      out.dop_pdop = in.dop.pdop;
    }
    if constexpr (has_hdop<T_dop>::value) {
      out.dop_hdop = in.dop.hdop;
    }
    if constexpr (has_vdop<T_dop>::value) {
      out.dop_vdop = in.dop.vdop;
    }
    if constexpr (has_tdop<T_dop>::value) {
      out.dop_tdop = in.dop.tdop;
    }
    if constexpr (has_gdop<T_dop>::value) {
      out.dop_gdop = in.dop.gdop;
    }
  }
  if constexpr (has_skyview_time<T>::value) {
    out.skyview_time.sec = static_cast<int32_t>(in.skyview_time.tv_sec);
    out.skyview_time.nanosec = static_cast<uint32_t>(in.skyview_time.tv_nsec);
  }
  if constexpr (has_satellites_visible<T>::value) {
    out.satellites_visible = in.satellites_visible;
  }
  if constexpr (has_dev<T>::value) {
    using T_dev = std::decay_t<decltype(in.dev)>;
    if constexpr (has_path<T_dev>::value) {
      out.dev_path.assign(in.dev.path, strnlen(in.dev.path, sizeof(in.dev.path)));
    }
    if constexpr (has_flags<T_dev>::value) {
      out.dev_flags = in.dev.flags;
    }
    if constexpr (has_driver<T_dev>::value) {
      out.dev_driver.assign(in.dev.driver, strnlen(in.dev.driver, sizeof(in.dev.driver)));
    }
    if constexpr (has_subtype<T_dev>::value) {
      out.dev_subtype.assign(in.dev.subtype, strnlen(in.dev.subtype, sizeof(in.dev.subtype)));
    }
    if constexpr (has_subtype1<T_dev>::value) {
      out.dev_subtype1.assign(in.dev.subtype1, strnlen(in.dev.subtype1, sizeof(in.dev.subtype1)));
    }
    if constexpr (has_sernum<T_dev>::value) {
      out.dev_sernum.assign(in.dev.sernum, strnlen(in.dev.sernum, sizeof(in.dev.sernum)));
    }
    if constexpr (has_hexdata<T_dev>::value) {
      out.dev_hexdata.assign(in.dev.hexdata, strnlen(in.dev.hexdata, sizeof(in.dev.hexdata)));
    }
    if constexpr (has_activated<T_dev>::value) {
      out.dev_activated.sec = static_cast<int32_t>(in.dev.activated.tv_sec);
      out.dev_activated.nanosec = static_cast<uint32_t>(in.dev.activated.tv_nsec);
    }
    if constexpr (has_baudrate<T_dev>::value) {
      out.dev_baudrate = in.dev.baudrate;
    }
    if constexpr (has_stopbits<T_dev>::value) {
      out.dev_stopbits = in.dev.stopbits;
    }
    if constexpr (has_parity<T_dev>::value) {
      out.dev_parity = in.dev.parity;
    }
    if constexpr (has_cycle<T_dev>::value) {
      out.dev_cycle.sec = static_cast<int32_t>(in.dev.cycle.tv_sec);
      out.dev_cycle.nanosec = static_cast<uint32_t>(in.dev.cycle.tv_nsec);
    }
    if constexpr (has_mincycle<T_dev>::value) {
      out.dev_mincycle.sec = static_cast<int32_t>(in.dev.mincycle.tv_sec);
      out.dev_mincycle.nanosec = static_cast<uint32_t>(in.dev.mincycle.tv_nsec);
    }
    if constexpr (has_driver_mode<T_dev>::value) {
      out.dev_driver_mode = in.dev.driver_mode;
    }
  }
  if constexpr (has_policy<T>::value) {
    using T_policy = std::decay_t<decltype(in.policy)>;
    if constexpr (has_watcher<T_policy>::value) {
      out.policy_watcher = in.policy.watcher;
    }
    if constexpr (has_json<T_policy>::value) {
      out.policy_json = in.policy.json;
    }
    if constexpr (has_nmea<T_policy>::value) {
      out.policy_nmea = in.policy.nmea;
    }
    if constexpr (has_raw<T_policy>::value) {
      out.policy_raw = in.policy.raw;
    }
    if constexpr (has_scaled<T_policy>::value) {
      out.policy_scaled = in.policy.scaled;
    }
    if constexpr (has_timing<T_policy>::value) {
      out.policy_timing = in.policy.timing;
    }
    if constexpr (has_split24<T_policy>::value) {
      out.policy_split24 = in.policy.split24;
    }
    if constexpr (has_pps<T_policy>::value) {
      out.policy_pps = in.policy.pps;
    }
    if constexpr (has_loglevel<T_policy>::value) {
      out.policy_loglevel = in.policy.loglevel;
    }
    if constexpr (has_devpath<T_policy>::value) {
      out.policy_devpath.assign(in.policy.devpath, strnlen(in.policy.devpath, sizeof(in.policy.devpath)));
    }
    if constexpr (has_remote<T_policy>::value) {
      out.policy_remote.assign(in.policy.remote, strnlen(in.policy.remote, sizeof(in.policy.remote)));
    }
  }
  if constexpr (has_devices<T>::value) {
    using T_devices = std::decay_t<decltype(in.devices)>;
    if constexpr (has_time<T_devices>::value) {
      out.devices_time.sec = static_cast<int32_t>(in.devices.time.tv_sec);
      out.devices_time.nanosec = static_cast<uint32_t>(in.devices.time.tv_nsec);
    }
    if constexpr (has_ndevices<T_devices>::value) {
      out.devices_ndevices = in.devices.ndevices;
    }
  }
  if constexpr (has_gst<T>::value) {
    using T_gst = std::decay_t<decltype(in.gst)>;
    if constexpr (has_utctime<T_gst>::value) {
      out.gst_utctime.sec = static_cast<int32_t>(in.gst.utctime.tv_sec);
      out.gst_utctime.nanosec = static_cast<uint32_t>(in.gst.utctime.tv_nsec);
    }
    if constexpr (has_rms_deviation<T_gst>::value) {
      out.gst_rms_deviation = in.gst.rms_deviation;
    }
    if constexpr (has_smajor_deviation<T_gst>::value) {
      out.gst_smajor_deviation = in.gst.smajor_deviation;
    }
    if constexpr (has_sminor_deviation<T_gst>::value) {
      out.gst_sminor_deviation = in.gst.sminor_deviation;
    }
    if constexpr (has_smajor_orientation<T_gst>::value) {
      out.gst_smajor_orientation = in.gst.smajor_orientation;
    }
    if constexpr (has_lat_err_deviation<T_gst>::value) {
      out.gst_lat_err_deviation = in.gst.lat_err_deviation;
    }
    if constexpr (has_lon_err_deviation<T_gst>::value) {
      out.gst_lon_err_deviation = in.gst.lon_err_deviation;
    }
    if constexpr (has_alt_err_deviation<T_gst>::value) {
      out.gst_alt_err_deviation = in.gst.alt_err_deviation;
    }
    if constexpr (has_ve_err_deviation<T_gst>::value) {
      out.gst_ve_err_deviation = in.gst.ve_err_deviation;
    }
    if constexpr (has_vn_err_deviation<T_gst>::value) {
      out.gst_vn_err_deviation = in.gst.vn_err_deviation;
    }
    if constexpr (has_vu_err_deviation<T_gst>::value) {
      out.gst_vu_err_deviation = in.gst.vu_err_deviation;
    }
  }
  if constexpr (has_raw<T>::value) {
    if (0 != (in.set & RAW_SET)) {
      using T_raw = std::decay_t<decltype(in.raw)>;
      if constexpr (has_mtime<T_raw>::value) {
        out.raw_mtime.sec = static_cast<int32_t>(in.raw.mtime.tv_sec);
        out.raw_mtime.nanosec = static_cast<uint32_t>(in.raw.mtime.tv_nsec);
      }
    }
  }
  if constexpr (has_osc<T>::value) {
    if (0 != (in.set & OSCILLATOR_SET)) {
      using T_osc = std::decay_t<decltype(in.osc)>;
      if constexpr (has_running<T_osc>::value) {
        out.osc_running = in.osc.running;
      }
      if constexpr (has_reference<T_osc>::value) {
        out.osc_reference = in.osc.reference;
      }
      if constexpr (has_disciplined<T_osc>::value) {
        out.osc_disciplined = in.osc.disciplined;
      }
      if constexpr (has_delta<T_osc>::value) {
        out.osc_delta = in.osc.delta;
      }
    }
  }
  if constexpr (has_version<T>::value) {
    if (0 != (in.set & VERSION_SET)) {
      using T_version = std::decay_t<decltype(in.version)>;
      if constexpr (has_release<T_version>::value) {
        out.version_release.assign(in.version.release, strnlen(in.version.release, sizeof(in.version.release)));
      }
      if constexpr (has_rev<T_version>::value) {
        out.version_rev.assign(in.version.rev, strnlen(in.version.rev, sizeof(in.version.rev)));
      }
      if constexpr (has_proto_major<T_version>::value) {
        out.version_proto_major = in.version.proto_major;
      }
      if constexpr (has_proto_minor<T_version>::value) {
        out.version_proto_minor = in.version.proto_minor;
      }
      if constexpr (has_remote<T_version>::value) {
        out.version_remote.assign(in.version.remote, strnlen(in.version.remote, sizeof(in.version.remote)));
      }
    }
  }
  if constexpr (has_error<T>::value) {
    if (0 != (in.set & ERROR_SET)) {
      out.error.assign(in.error, strnlen(in.error, sizeof(in.error)));
    }
  }
  if constexpr (has_attitude<T>::value) {
    using T_attitude = std::decay_t<decltype(in.attitude)>;
    if constexpr (has_mtime<T_attitude>::value) {
      out.attitude_mtime.sec = static_cast<int32_t>(in.attitude.mtime.tv_sec);
      out.attitude_mtime.nanosec = static_cast<uint32_t>(in.attitude.mtime.tv_nsec);
    }
    if constexpr (has_timeTag<T_attitude>::value) {
      out.attitude_time_tag = in.attitude.timeTag;
    }
    if constexpr (has_msg<T_attitude>::value) {
      out.attitude_msg.assign(in.attitude.msg, strnlen(in.attitude.msg, sizeof(in.attitude.msg)));
    }
    if constexpr (has_acc_len<T_attitude>::value) {
      out.attitude_acc_len = in.attitude.acc_len;
    }
    if constexpr (has_acc_x<T_attitude>::value) {
      out.attitude_acc_x = in.attitude.acc_x;
    }
    if constexpr (has_acc_y<T_attitude>::value) {
      out.attitude_acc_y = in.attitude.acc_y;
    }
    if constexpr (has_acc_z<T_attitude>::value) {
      out.attitude_acc_z = in.attitude.acc_z;
    }
    if constexpr (has_depth<T_attitude>::value) {
      out.attitude_depth = in.attitude.depth;
    }
    if constexpr (has_dip<T_attitude>::value) {
      out.attitude_dip = in.attitude.dip;
    }
    if constexpr (has_gyro_temp<T_attitude>::value) {
      out.attitude_gyro_temp = in.attitude.gyro_temp;
    }
    if constexpr (has_gyro_x<T_attitude>::value) {
      out.attitude_gyro_x = in.attitude.gyro_x;
    }
    if constexpr (has_gyro_y<T_attitude>::value) {
      out.attitude_gyro_y = in.attitude.gyro_y;
    }
    if constexpr (has_gyro_z<T_attitude>::value) {
      out.attitude_gyro_z = in.attitude.gyro_z;
    }
    if constexpr (has_heading<T_attitude>::value) {
      out.attitude_heading = in.attitude.heading;
    }
    if constexpr (has_mheading<T_attitude>::value) {
      out.attitude_mheading = in.attitude.mheading;
    }
    if constexpr (has_mag_len<T_attitude>::value) {
      out.attitude_mag_len = in.attitude.mag_len;
    }
    if constexpr (has_mag_x<T_attitude>::value) {
      out.attitude_mag_x = in.attitude.mag_x;
    }
    if constexpr (has_mag_y<T_attitude>::value) {
      out.attitude_mag_y = in.attitude.mag_y;
    }
    if constexpr (has_mag_z<T_attitude>::value) {
      out.attitude_mag_z = in.attitude.mag_z;
    }
    if constexpr (has_pitch<T_attitude>::value) {
      out.attitude_pitch = in.attitude.pitch;
    }
    if constexpr (has_roll<T_attitude>::value) {
      out.attitude_roll = in.attitude.roll;
    }
    if constexpr (has_rot<T_attitude>::value) {
      out.attitude_rot = in.attitude.rot;
    }
    if constexpr (has_temp<T_attitude>::value) {
      out.attitude_temp = in.attitude.temp;
    }
    if constexpr (has_yaw<T_attitude>::value) {
      out.attitude_yaw = in.attitude.yaw;
    }
    if constexpr (has_mag_st<T_attitude>::value) {
      out.attitude_mag_st = in.attitude.mag_st;
    }
    if constexpr (has_pitch_st<T_attitude>::value) {
      out.attitude_pitch_st = in.attitude.pitch_st;
    }
    if constexpr (has_roll_st<T_attitude>::value) {
      out.attitude_roll_st = in.attitude.roll_st;
    }
    if constexpr (has_yaw_st<T_attitude>::value) {
      out.attitude_yaw_st = in.attitude.yaw_st;
    }
    if constexpr (has_base<T_attitude>::value) {
      using T_attitude_base = std::decay_t<decltype(in.attitude.base)>;
      if constexpr (has_status<T_attitude_base>::value) {
        out.attitude_base_status = in.attitude.base.status;
      }
      if constexpr (has_east<T_attitude_base>::value) {
        out.attitude_base_east = in.attitude.base.east;
      }
      if constexpr (has_north<T_attitude_base>::value) {
        out.attitude_base_north = in.attitude.base.north;
      }
      if constexpr (has_up<T_attitude_base>::value) {
        out.attitude_base_up = in.attitude.base.up;
      }
      if constexpr (has_length<T_attitude_base>::value) {
        out.attitude_base_length = in.attitude.base.length;
      }
      if constexpr (has_course<T_attitude_base>::value) {
        out.attitude_base_course = in.attitude.base.course;
      }
      if constexpr (has_ratio<T_attitude_base>::value) {
        out.attitude_base_ratio = in.attitude.base.ratio;
      }
    }
  }
  if constexpr (has_toff<T>::value) {
    using T_toff = std::decay_t<decltype(in.toff)>;
    if constexpr (has_real<T_toff>::value) {
      out.toff_real.sec = static_cast<int32_t>(in.toff.real.tv_sec);
      out.toff_real.nanosec = static_cast<uint32_t>(in.toff.real.tv_nsec);
    }
    if constexpr (has_clock<T_toff>::value) {
      out.toff_clock.sec = static_cast<int32_t>(in.toff.clock.tv_sec);
      out.toff_clock.nanosec = static_cast<uint32_t>(in.toff.clock.tv_nsec);
    }
  }
  if constexpr (has_pps<T>::value) {
    using T_pps = std::decay_t<decltype(in.pps)>;
    if constexpr (has_real<T_pps>::value) {
      out.pps_real.sec = static_cast<int32_t>(in.pps.real.tv_sec);
      out.pps_real.nanosec = static_cast<uint32_t>(in.pps.real.tv_nsec);
    }
    if constexpr (has_clock<T_pps>::value) {
      out.pps_clock.sec = static_cast<int32_t>(in.pps.clock.tv_sec);
      out.pps_clock.nanosec = static_cast<uint32_t>(in.pps.clock.tv_nsec);
    }
  }
  if constexpr (has_qErr<T>::value) {
    out.q_err = in.qErr;
  }
  if constexpr (has_qErr_time<T>::value) {
    out.q_err_time.sec = static_cast<int32_t>(in.qErr_time.tv_sec);
    out.q_err_time.nanosec = static_cast<uint32_t>(in.qErr_time.tv_nsec);
  }
  if constexpr (has_source<T>::value) {
    using T_source = std::decay_t<decltype(in.source)>;
    if constexpr (has_spec<T_source>::value) {
      out.source_spec.assign(in.source.spec, strnlen(in.source.spec, sizeof(in.source.spec)));
    }
  }
  if constexpr (has_watch<T>::value) {
    out.watch = in.watch;
  }
}

/// Fill the skyview_* arrays from in.skyview, taking the
/// elements named by idx. One loop, so every array in the group ends
/// the same length.
template <typename T>
inline void fill_skyview(const T& in, gps_extended_msgs::msg::GPSDRaw16v1& out,
                          const std::vector<std::size_t>& idx)
{
  (void)in;
  (void)out;
  (void)idx;
  const std::size_t count = idx.size();
  if constexpr (has_skyview<T>::value) {
    using T_skyview = std::decay_t<decltype(in.skyview)>;
    out.skyview_ss.resize(count);
    out.skyview_prn.resize(count);
    out.skyview_elevation.resize(count);
    out.skyview_azimuth.resize(count);
    out.skyview_pr_res.resize(count);
    out.skyview_pr_rate.resize(count);
    out.skyview_pr.resize(count);
    out.skyview_used.resize(count);
    out.skyview_quality_ind.resize(count);
    out.skyview_gnssid.resize(count);
    out.skyview_svid.resize(count);
    out.skyview_sigid.resize(count);
    out.skyview_freqid.resize(count);
    out.skyview_health.resize(count);
    for (std::size_t i = 0; i < count; ++i) {
      const std::size_t src = idx[i];
      using T_elem_skyview_ss = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_ss<T_elem_skyview_ss>::value) {
        out.skyview_ss[i] = in.skyview[src].ss;
      }
      using T_elem_skyview_prn = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_PRN<T_elem_skyview_prn>::value) {
        out.skyview_prn[i] = in.skyview[src].PRN;
      }
      using T_elem_skyview_elevation = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_elevation<T_elem_skyview_elevation>::value) {
        out.skyview_elevation[i] = in.skyview[src].elevation;
      }
      using T_elem_skyview_azimuth = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_azimuth<T_elem_skyview_azimuth>::value) {
        out.skyview_azimuth[i] = in.skyview[src].azimuth;
      }
      using T_elem_skyview_pr_res = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_prRes<T_elem_skyview_pr_res>::value) {
        out.skyview_pr_res[i] = in.skyview[src].prRes;
      }
      using T_elem_skyview_pr_rate = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_prRate<T_elem_skyview_pr_rate>::value) {
        out.skyview_pr_rate[i] = in.skyview[src].prRate;
      }
      using T_elem_skyview_pr = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_pr<T_elem_skyview_pr>::value) {
        out.skyview_pr[i] = in.skyview[src].pr;
      }
      using T_elem_skyview_used = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_used<T_elem_skyview_used>::value) {
        out.skyview_used[i] = in.skyview[src].used;
      }
      using T_elem_skyview_quality_ind = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_qualityInd<T_elem_skyview_quality_ind>::value) {
        out.skyview_quality_ind[i] = in.skyview[src].qualityInd;
      }
      using T_elem_skyview_gnssid = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_gnssid<T_elem_skyview_gnssid>::value) {
        out.skyview_gnssid[i] = in.skyview[src].gnssid;
      }
      using T_elem_skyview_svid = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_svid<T_elem_skyview_svid>::value) {
        out.skyview_svid[i] = in.skyview[src].svid;
      }
      using T_elem_skyview_sigid = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_sigid<T_elem_skyview_sigid>::value) {
        out.skyview_sigid[i] = in.skyview[src].sigid;
      }
      using T_elem_skyview_freqid = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_freqid<T_elem_skyview_freqid>::value) {
        out.skyview_freqid[i] = in.skyview[src].freqid;
      }
      using T_elem_skyview_health = std::decay_t<decltype(in.skyview[src])>;
      if constexpr (has_health<T_elem_skyview_health>::value) {
        out.skyview_health[i] = in.skyview[src].health;
      }
    }
  }
}

/// Fill the devices_list_* arrays from in.devices.list, taking the
/// elements named by idx. One loop, so every array in the group ends
/// the same length.
template <typename T>
inline void fill_devices_list(const T& in, gps_extended_msgs::msg::GPSDRaw16v1& out,
                          const std::vector<std::size_t>& idx)
{
  (void)in;
  (void)out;
  (void)idx;
  const std::size_t count = idx.size();
  if constexpr (has_devices<T>::value) {
    using T_devices = std::decay_t<decltype(in.devices)>;
  if constexpr (has_list<T_devices>::value) {
    using T_devices_list = std::decay_t<decltype(in.devices.list)>;
    out.devices_list_path.resize(count);
    out.devices_list_flags.resize(count);
    out.devices_list_driver.resize(count);
    out.devices_list_subtype.resize(count);
    out.devices_list_subtype1.resize(count);
    out.devices_list_sernum.resize(count);
    out.devices_list_hexdata.resize(count);
    out.devices_list_activated.resize(count);
    out.devices_list_baudrate.resize(count);
    out.devices_list_stopbits.resize(count);
    out.devices_list_parity.resize(count);
    out.devices_list_cycle.resize(count);
    out.devices_list_mincycle.resize(count);
    out.devices_list_driver_mode.resize(count);
    for (std::size_t i = 0; i < count; ++i) {
      const std::size_t src = idx[i];
      using T_elem_devices_list_path = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_path<T_elem_devices_list_path>::value) {
        out.devices_list_path[i].assign(in.devices.list[src].path, strnlen(in.devices.list[src].path, sizeof(in.devices.list[src].path)));
      }
      using T_elem_devices_list_flags = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_flags<T_elem_devices_list_flags>::value) {
        out.devices_list_flags[i] = in.devices.list[src].flags;
      }
      using T_elem_devices_list_driver = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_driver<T_elem_devices_list_driver>::value) {
        out.devices_list_driver[i].assign(in.devices.list[src].driver, strnlen(in.devices.list[src].driver, sizeof(in.devices.list[src].driver)));
      }
      using T_elem_devices_list_subtype = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_subtype<T_elem_devices_list_subtype>::value) {
        out.devices_list_subtype[i].assign(in.devices.list[src].subtype, strnlen(in.devices.list[src].subtype, sizeof(in.devices.list[src].subtype)));
      }
      using T_elem_devices_list_subtype1 = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_subtype1<T_elem_devices_list_subtype1>::value) {
        out.devices_list_subtype1[i].assign(in.devices.list[src].subtype1, strnlen(in.devices.list[src].subtype1, sizeof(in.devices.list[src].subtype1)));
      }
      using T_elem_devices_list_sernum = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_sernum<T_elem_devices_list_sernum>::value) {
        out.devices_list_sernum[i].assign(in.devices.list[src].sernum, strnlen(in.devices.list[src].sernum, sizeof(in.devices.list[src].sernum)));
      }
      using T_elem_devices_list_hexdata = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_hexdata<T_elem_devices_list_hexdata>::value) {
        out.devices_list_hexdata[i].assign(in.devices.list[src].hexdata, strnlen(in.devices.list[src].hexdata, sizeof(in.devices.list[src].hexdata)));
      }
      using T_elem_devices_list_activated = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_activated<T_elem_devices_list_activated>::value) {
        out.devices_list_activated[i].sec = static_cast<int32_t>(in.devices.list[src].activated.tv_sec);
        out.devices_list_activated[i].nanosec = static_cast<uint32_t>(in.devices.list[src].activated.tv_nsec);
      }
      using T_elem_devices_list_baudrate = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_baudrate<T_elem_devices_list_baudrate>::value) {
        out.devices_list_baudrate[i] = in.devices.list[src].baudrate;
      }
      using T_elem_devices_list_stopbits = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_stopbits<T_elem_devices_list_stopbits>::value) {
        out.devices_list_stopbits[i] = in.devices.list[src].stopbits;
      }
      using T_elem_devices_list_parity = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_parity<T_elem_devices_list_parity>::value) {
        out.devices_list_parity[i] = in.devices.list[src].parity;
      }
      using T_elem_devices_list_cycle = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_cycle<T_elem_devices_list_cycle>::value) {
        out.devices_list_cycle[i].sec = static_cast<int32_t>(in.devices.list[src].cycle.tv_sec);
        out.devices_list_cycle[i].nanosec = static_cast<uint32_t>(in.devices.list[src].cycle.tv_nsec);
      }
      using T_elem_devices_list_mincycle = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_mincycle<T_elem_devices_list_mincycle>::value) {
        out.devices_list_mincycle[i].sec = static_cast<int32_t>(in.devices.list[src].mincycle.tv_sec);
        out.devices_list_mincycle[i].nanosec = static_cast<uint32_t>(in.devices.list[src].mincycle.tv_nsec);
      }
      using T_elem_devices_list_driver_mode = std::decay_t<decltype(in.devices.list[src])>;
      if constexpr (has_driver_mode<T_elem_devices_list_driver_mode>::value) {
        out.devices_list_driver_mode[i] = in.devices.list[src].driver_mode;
      }
    }
  }
  }
}

/// Fill the raw_meas_* arrays from in.raw.meas, taking the
/// elements named by idx. One loop, so every array in the group ends
/// the same length.
template <typename T>
inline void fill_raw_meas(const T& in, gps_extended_msgs::msg::GPSDRaw16v1& out,
                          const std::vector<std::size_t>& idx)
{
  (void)in;
  (void)out;
  (void)idx;
  const std::size_t count = idx.size();
  if constexpr (has_raw<T>::value) {
    using T_raw = std::decay_t<decltype(in.raw)>;
  if constexpr (has_meas<T_raw>::value) {
    using T_raw_meas = std::decay_t<decltype(in.raw.meas)>;
    if (0 != (in.set & RAW_SET)) {
      out.raw_meas_gnssid.resize(count);
      out.raw_meas_svid.resize(count);
      out.raw_meas_sigid.resize(count);
      out.raw_meas_snr.resize(count);
      out.raw_meas_freqid.resize(count);
      out.raw_meas_lli.resize(count);
      out.raw_meas_obs_code.resize(count);
      out.raw_meas_codephase.resize(count);
      out.raw_meas_carrierphase.resize(count);
      out.raw_meas_pseudorange.resize(count);
      out.raw_meas_deltarange.resize(count);
      out.raw_meas_doppler.resize(count);
      out.raw_meas_locktime.resize(count);
      out.raw_meas_l2c.resize(count);
      out.raw_meas_c2c.resize(count);
      out.raw_meas_satstat.resize(count);
      for (std::size_t i = 0; i < count; ++i) {
        const std::size_t src = idx[i];
        using T_elem_raw_meas_gnssid = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_gnssid<T_elem_raw_meas_gnssid>::value) {
          out.raw_meas_gnssid[i] = in.raw.meas[src].gnssid;
        }
        using T_elem_raw_meas_svid = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_svid<T_elem_raw_meas_svid>::value) {
          out.raw_meas_svid[i] = in.raw.meas[src].svid;
        }
        using T_elem_raw_meas_sigid = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_sigid<T_elem_raw_meas_sigid>::value) {
          out.raw_meas_sigid[i] = in.raw.meas[src].sigid;
        }
        using T_elem_raw_meas_snr = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_snr<T_elem_raw_meas_snr>::value) {
          out.raw_meas_snr[i] = in.raw.meas[src].snr;
        }
        using T_elem_raw_meas_freqid = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_freqid<T_elem_raw_meas_freqid>::value) {
          out.raw_meas_freqid[i] = in.raw.meas[src].freqid;
        }
        using T_elem_raw_meas_lli = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_lli<T_elem_raw_meas_lli>::value) {
          out.raw_meas_lli[i] = in.raw.meas[src].lli;
        }
        using T_elem_raw_meas_obs_code = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_obs_code<T_elem_raw_meas_obs_code>::value) {
          out.raw_meas_obs_code[i].assign(in.raw.meas[src].obs_code, strnlen(in.raw.meas[src].obs_code, sizeof(in.raw.meas[src].obs_code)));
        }
        using T_elem_raw_meas_codephase = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_codephase<T_elem_raw_meas_codephase>::value) {
          out.raw_meas_codephase[i] = in.raw.meas[src].codephase;
        }
        using T_elem_raw_meas_carrierphase = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_carrierphase<T_elem_raw_meas_carrierphase>::value) {
          out.raw_meas_carrierphase[i] = in.raw.meas[src].carrierphase;
        }
        using T_elem_raw_meas_pseudorange = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_pseudorange<T_elem_raw_meas_pseudorange>::value) {
          out.raw_meas_pseudorange[i] = in.raw.meas[src].pseudorange;
        }
        using T_elem_raw_meas_deltarange = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_deltarange<T_elem_raw_meas_deltarange>::value) {
          out.raw_meas_deltarange[i] = in.raw.meas[src].deltarange;
        }
        using T_elem_raw_meas_doppler = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_doppler<T_elem_raw_meas_doppler>::value) {
          out.raw_meas_doppler[i] = in.raw.meas[src].doppler;
        }
        using T_elem_raw_meas_locktime = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_locktime<T_elem_raw_meas_locktime>::value) {
          out.raw_meas_locktime[i] = in.raw.meas[src].locktime;
        }
        using T_elem_raw_meas_l2c = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_l2c<T_elem_raw_meas_l2c>::value) {
          out.raw_meas_l2c[i] = in.raw.meas[src].l2c;
        }
        using T_elem_raw_meas_c2c = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_c2c<T_elem_raw_meas_c2c>::value) {
          out.raw_meas_c2c[i] = in.raw.meas[src].c2c;
        }
        using T_elem_raw_meas_satstat = std::decay_t<decltype(in.raw.meas[src])>;
        if constexpr (has_satstat<T_elem_raw_meas_satstat>::value) {
          out.raw_meas_satstat[i] = in.raw.meas[src].satstat;
        }
      }
    }
  }
  }
}

/// Fill the imu_* arrays from in.imu, taking the
/// elements named by idx. One loop, so every array in the group ends
/// the same length.
template <typename T>
inline void fill_imu(const T& in, gps_extended_msgs::msg::GPSDRaw16v1& out,
                          const std::vector<std::size_t>& idx)
{
  (void)in;
  (void)out;
  (void)idx;
  const std::size_t count = idx.size();
  if constexpr (has_imu<T>::value) {
    using T_imu = std::decay_t<decltype(in.imu)>;
    out.imu_mtime.resize(count);
    out.imu_time_tag.resize(count);
    out.imu_msg.resize(count);
    out.imu_acc_len.resize(count);
    out.imu_acc_x.resize(count);
    out.imu_acc_y.resize(count);
    out.imu_acc_z.resize(count);
    out.imu_depth.resize(count);
    out.imu_dip.resize(count);
    out.imu_gyro_temp.resize(count);
    out.imu_gyro_x.resize(count);
    out.imu_gyro_y.resize(count);
    out.imu_gyro_z.resize(count);
    out.imu_heading.resize(count);
    out.imu_mheading.resize(count);
    out.imu_mag_len.resize(count);
    out.imu_mag_x.resize(count);
    out.imu_mag_y.resize(count);
    out.imu_mag_z.resize(count);
    out.imu_pitch.resize(count);
    out.imu_roll.resize(count);
    out.imu_rot.resize(count);
    out.imu_temp.resize(count);
    out.imu_yaw.resize(count);
    out.imu_mag_st.resize(count);
    out.imu_pitch_st.resize(count);
    out.imu_roll_st.resize(count);
    out.imu_yaw_st.resize(count);
    out.imu_base_status.resize(count);
    out.imu_base_east.resize(count);
    out.imu_base_north.resize(count);
    out.imu_base_up.resize(count);
    out.imu_base_length.resize(count);
    out.imu_base_course.resize(count);
    out.imu_base_ratio.resize(count);
    for (std::size_t i = 0; i < count; ++i) {
      const std::size_t src = idx[i];
      using T_elem_imu_mtime = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_mtime<T_elem_imu_mtime>::value) {
        out.imu_mtime[i].sec = static_cast<int32_t>(in.imu[src].mtime.tv_sec);
        out.imu_mtime[i].nanosec = static_cast<uint32_t>(in.imu[src].mtime.tv_nsec);
      }
      using T_elem_imu_time_tag = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_timeTag<T_elem_imu_time_tag>::value) {
        out.imu_time_tag[i] = in.imu[src].timeTag;
      }
      using T_elem_imu_msg = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_msg<T_elem_imu_msg>::value) {
        out.imu_msg[i].assign(in.imu[src].msg, strnlen(in.imu[src].msg, sizeof(in.imu[src].msg)));
      }
      using T_elem_imu_acc_len = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_acc_len<T_elem_imu_acc_len>::value) {
        out.imu_acc_len[i] = in.imu[src].acc_len;
      }
      using T_elem_imu_acc_x = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_acc_x<T_elem_imu_acc_x>::value) {
        out.imu_acc_x[i] = in.imu[src].acc_x;
      }
      using T_elem_imu_acc_y = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_acc_y<T_elem_imu_acc_y>::value) {
        out.imu_acc_y[i] = in.imu[src].acc_y;
      }
      using T_elem_imu_acc_z = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_acc_z<T_elem_imu_acc_z>::value) {
        out.imu_acc_z[i] = in.imu[src].acc_z;
      }
      using T_elem_imu_depth = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_depth<T_elem_imu_depth>::value) {
        out.imu_depth[i] = in.imu[src].depth;
      }
      using T_elem_imu_dip = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_dip<T_elem_imu_dip>::value) {
        out.imu_dip[i] = in.imu[src].dip;
      }
      using T_elem_imu_gyro_temp = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_gyro_temp<T_elem_imu_gyro_temp>::value) {
        out.imu_gyro_temp[i] = in.imu[src].gyro_temp;
      }
      using T_elem_imu_gyro_x = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_gyro_x<T_elem_imu_gyro_x>::value) {
        out.imu_gyro_x[i] = in.imu[src].gyro_x;
      }
      using T_elem_imu_gyro_y = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_gyro_y<T_elem_imu_gyro_y>::value) {
        out.imu_gyro_y[i] = in.imu[src].gyro_y;
      }
      using T_elem_imu_gyro_z = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_gyro_z<T_elem_imu_gyro_z>::value) {
        out.imu_gyro_z[i] = in.imu[src].gyro_z;
      }
      using T_elem_imu_heading = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_heading<T_elem_imu_heading>::value) {
        out.imu_heading[i] = in.imu[src].heading;
      }
      using T_elem_imu_mheading = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_mheading<T_elem_imu_mheading>::value) {
        out.imu_mheading[i] = in.imu[src].mheading;
      }
      using T_elem_imu_mag_len = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_mag_len<T_elem_imu_mag_len>::value) {
        out.imu_mag_len[i] = in.imu[src].mag_len;
      }
      using T_elem_imu_mag_x = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_mag_x<T_elem_imu_mag_x>::value) {
        out.imu_mag_x[i] = in.imu[src].mag_x;
      }
      using T_elem_imu_mag_y = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_mag_y<T_elem_imu_mag_y>::value) {
        out.imu_mag_y[i] = in.imu[src].mag_y;
      }
      using T_elem_imu_mag_z = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_mag_z<T_elem_imu_mag_z>::value) {
        out.imu_mag_z[i] = in.imu[src].mag_z;
      }
      using T_elem_imu_pitch = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_pitch<T_elem_imu_pitch>::value) {
        out.imu_pitch[i] = in.imu[src].pitch;
      }
      using T_elem_imu_roll = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_roll<T_elem_imu_roll>::value) {
        out.imu_roll[i] = in.imu[src].roll;
      }
      using T_elem_imu_rot = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_rot<T_elem_imu_rot>::value) {
        out.imu_rot[i] = in.imu[src].rot;
      }
      using T_elem_imu_temp = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_temp<T_elem_imu_temp>::value) {
        out.imu_temp[i] = in.imu[src].temp;
      }
      using T_elem_imu_yaw = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_yaw<T_elem_imu_yaw>::value) {
        out.imu_yaw[i] = in.imu[src].yaw;
      }
      using T_elem_imu_mag_st = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_mag_st<T_elem_imu_mag_st>::value) {
        out.imu_mag_st[i] = in.imu[src].mag_st;
      }
      using T_elem_imu_pitch_st = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_pitch_st<T_elem_imu_pitch_st>::value) {
        out.imu_pitch_st[i] = in.imu[src].pitch_st;
      }
      using T_elem_imu_roll_st = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_roll_st<T_elem_imu_roll_st>::value) {
        out.imu_roll_st[i] = in.imu[src].roll_st;
      }
      using T_elem_imu_yaw_st = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_yaw_st<T_elem_imu_yaw_st>::value) {
        out.imu_yaw_st[i] = in.imu[src].yaw_st;
      }
      using T_elem_imu_base_status = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_base<T_elem_imu_base_status>::value) {
        out.imu_base_status[i] = in.imu[src].base.status;
      }
      using T_elem_imu_base_east = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_base<T_elem_imu_base_east>::value) {
        out.imu_base_east[i] = in.imu[src].base.east;
      }
      using T_elem_imu_base_north = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_base<T_elem_imu_base_north>::value) {
        out.imu_base_north[i] = in.imu[src].base.north;
      }
      using T_elem_imu_base_up = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_base<T_elem_imu_base_up>::value) {
        out.imu_base_up[i] = in.imu[src].base.up;
      }
      using T_elem_imu_base_length = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_base<T_elem_imu_base_length>::value) {
        out.imu_base_length[i] = in.imu[src].base.length;
      }
      using T_elem_imu_base_course = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_base<T_elem_imu_base_course>::value) {
        out.imu_base_course[i] = in.imu[src].base.course;
      }
      using T_elem_imu_base_ratio = std::decay_t<decltype(in.imu[src])>;
      if constexpr (has_base<T_elem_imu_base_ratio>::value) {
        out.imu_base_ratio[i] = in.imu[src].base.ratio;
      }
    }
  }
}

}  // namespace generated
}  // namespace gpsd_client

#endif  // GPSD_RAW_FILL_MAJOR == 16 ...

#endif  // GPSD_CLIENT__PARSERS__GENERATED__FILL_16V1_HPP_
